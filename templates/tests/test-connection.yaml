apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "scanagroempresa.fullname" . }}-test-connection"
  labels:
    {{- include "scanagroempresa.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "scanagroempresa.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
